import type { Metadata } from "next";
import { SITE_URL } from "@/lib/site";

/**
 * Метаданные отдельным layout, потому что сама страница клиентская: язык на
 * ней переключается на лету, а Next требует метаданные с сервера и статикой.
 *
 * Без этого файла страница представлялась в поиске заголовком главной —
 * «AI-агрегатор заявок», то есть текстом для заказчика. Исполнитель ищет
 * другое: «работа на манипуляторе», «заявки на самосвал», — и по такому
 * заголовку не кликнет, даже если найдёт.
 *
 * Русский язык здесь зафиксирован: статические метаданные не знают, какой
 * язык выберет посетитель, а поисковый трафик по этим запросам в основном
 * русскоязычный. Содержимое самой страницы переключается как обычно.
 */
export const metadata: Metadata = {
  title: "Заявки на спецтехнику для исполнителей — KerekTap",
  description:
    "Получайте заявки на манипулятор, автокран, самосвал и газель в WhatsApp. Комиссию с заказа не берём, с клиентом договариваетесь напрямую. Первые заявки бесплатно.",
  alternates: { canonical: `${SITE_URL}/dlya-ispolniteley` },
  openGraph: {
    title: "Заявки на спецтехнику для исполнителей — KerekTap",
    description:
      "Клиент оставляет заявку — мы присылаем её вам с телефоном заказчика. Звоните и договаривайтесь напрямую.",
    url: `${SITE_URL}/dlya-ispolniteley`,
    type: "website",
  },
};

export default function SuppliersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
